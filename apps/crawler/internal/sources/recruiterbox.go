package sources

// Recruiterbox was rebranded to Trakstar Hire in 2022 and the
// `*.recruiterbox.com/jobs` URL pattern is no longer hosted — even
// recruiterbox.com's own careers page now lives at
// recruiterbox.hire.trakstar.com. Trakstar's public board API is auth-gated
// (per-tenant token), so there is no scrape path equivalent to what
// Recruiterbox previously exposed.
//
// Stub-only file kept for grepability.
